import 'package:flutter/material.dart';

class AppColors {
  static const Color primaryColor = Color(0xFF0580FA);
  static const Color backgroundColor = Color(
    0xFF0F1A24,
  );
  static const Color surfaceColor = Color(
    0xFF172636,
  );
  static const Color cardColor = Color(0xFF21364A);
  static const Color textPrimaryColor = Color(0xFFFFFFFF);
  static const Color textSecondaryColor = Color(0xFF8FADCC);
  static const Color dividerColor = Color(0xFF2D3E4F);

  static const Color errorColor = Colors.red;
  static const Color successColor = Color(0xFF4CAF50);
  static const Color warningColor = Color(0xFFFF9800);
  static const Color infoColor = Color(0xFF2196F3);

  static const Color blackOverlay = Color(0x80000000);
  static const Color whiteOverlay = Color(0x80FFFFFF);

  AppColors._();
}
